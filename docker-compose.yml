services:
  cf-best-domain:
    image: ghcr.io/hideinmatrix/cf-best-domain:latest
    container_name: cf-best-domain
    restart: unless-stopped
    command:
      - -api
      - -update
      - -listen
      - ${CFBD_LISTEN:-:8080}
      - -interval
      - ${CFBD_INTERVAL:-30m}
    ports:
      - "${CFBD_API_PORT:-8080}:8080"
    env_file:
      - .env
    environment:
      TZ: Asia/Shanghai
    read_only: true
    security_opt:
      - no-new-privileges:true
